// kobel-services: system state providers (zbus/tokio on a side thread).
// Snapshots are plain data sent over channels; NEVER Freya types (UI-thread-only).
// Placeholder; see docs/FREYA-PLAN.md section 5.
